// Metro config for the JARFIS multi-domain monorepo fixture (M6.1).
// `app/` is the React Native (mobile) workspace.
const {getDefaultConfig} = require('@react-native/metro-config');
module.exports = getDefaultConfig(__dirname);
