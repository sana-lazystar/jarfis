# Phase 5: Review & QA — Agent Prompts

> This file contains agent prompts externalized from work.md.
> Auto-generated by distill.
> **Locale**: Present ALL user-facing output in $LOCALE language. Internal reasoning: English.

## Phase 4 Agent Status (Orchestrator Pre-Work)

> Before starting Phase 5, the orchestrator reads `phase4_agents` from `.jarfis-state.json`
> and injects the information below into each review agent prompt.

```
Injection Format:
─── Phase 4 Implementation Agent Status ───
Backend: $PHASE4_AGENTS.backend (completed | skipped | failed)
Frontend: $PHASE4_AGENTS.frontend (completed | skipped | failed)
DevOps: $PHASE4_AGENTS.devops (completed | skipped | failed)

⚠️ Code does not exist for "skipped" parts — exclude them from review.
⚠️ Code may be incomplete for "failed" parts — take this into account during review.
───────────────────────────────
```

> If the phase4_agents field is absent, omit this section and fall back to the N/A criteria in tasks.md.

---

Tech Lead (tech-lead):
```
Task prompt:
"Cross-reference $DOCS_DIR/api-spec.md against the actual implementation code to verify API Contract compliance.

📂 **Project Directories:**
- Backend: $BACKEND_PROJECT_DIR
- Frontend: $FRONTEND_PROJECT_DIR

Verification Items:
1. Extract actually implemented endpoint paths/methods from BE code → compare against api-spec.md
2. Extract actually called API paths/types from FE code → compare against api-spec.md
3. Per-item match/mismatch determination:
   - [ ] Endpoint path match
   - [ ] HTTP Method match
   - [ ] Request parameter match
   - [ ] Response schema compatibility
   - [ ] Error code match

Save results to $DOCS_DIR/api-contract-check.md.
Clearly flag any mismatches."
```

> **Reminder**: All output in $LOCALE language.

Tech Lead (tech-lead):
```
Task prompt:
"Review the code implemented in Phase 4.
⚠️ Check $DOCS_DIR/tasks.md and review only the parts that are not N/A (actually implemented). (+ see Phase 4 Agent Status)

📂 **Project Directories:**
- Backend: $BACKEND_PROJECT_DIR
- Frontend: $FRONTEND_PROJECT_DIR
(Ignore directories marked N/A)

Reference Documents:
- $DOCS_DIR/api-contract-check.md (if present — prioritize checking mismatches)
- $DOCS_DIR/deployment-plan.md (review appropriateness of deployment strategy)

Review Criteria:
- Code quality and readability
- Adherence to architecture design ($DOCS_DIR/architecture.md)
- API Contract compliance — reference api-contract-check.md results
- Appropriateness of design patterns
- Error handling
- Performance considerations — whether PRD Performance Budget criteria are met
- Technical debt
- Appropriateness of deployment strategy ($DOCS_DIR/deployment-plan.md)

Compile the review results."
```

QA (senior-qa-engineer):
```
Task prompt:
"Perform QA based on the code implemented in Phase 4.
⚠️ Check $DOCS_DIR/tasks.md and test only the parts that are not N/A (actually implemented). (+ see Phase 4 Agent Status)
If UX design ($DOCS_DIR/design/ or $DOCS_DIR/ux-direction.md) does not exist, exclude UI-related tests.

Reference Documents:
- PRD: $DOCS_DIR/prd.md
- UX Design: $DOCS_DIR/design/ (HTML mockups, only if present)
- Test Strategy: $DOCS_DIR/test-strategy.md (verify against this strategy)

─── TDD-Enabled Review Streamlining (phases.4.tdd_enabled === true) ───
Tests written by QA in Phase 4-0.5 are already in a passing state.
Since P0/P1 functional correctness scenarios from test-strategy.md have already been verified,
QA review should focus on:
- Integration/E2E level issues not covered by tests
- Non-functional requirements: performance testing (Performance Budget criteria), accessibility
- Review items reported by implementation agents via [TEST_ISSUE]
- Code quality and architecture fitness reviews are not reduced (remains Tech Lead's responsibility)

─── Test File Modification Verification (test_modifications) ───
If `.jarfis-state.json`'s `ratchet.phase4_tests.test_modifications` contains
records of implementation agents modifying test files:
1. Check the reason for each modification ([TEST_MODIFIED] tag in commit message)
2. Determine if the modification is justified: was it fixing a bug/inaccuracy in the test itself, or circumventing the test?
3. Unjustified modifications → report as "Test Integrity Violation" issue in review.md
4. If test_modifications is empty, skip this block
──────────────────────────────────────────

※ If tdd_enabled is false, ignore this block.
───────────────────────────────

QA Items:
- Pass/Fail determination per test scenario in test-strategy.md
- Edge case testing
- Cross-browser/device compatibility (only if Frontend exists)
- Performance testing — Pass/Fail determination against PRD Performance Budget criteria
- Accessibility testing (only if Frontend exists)

Compile the results."
```

> **Reminder**: All output in $LOCALE language.

Security (senior-security-engineer):
```
Task prompt:
"Perform a security review of the code implemented in Phase 4.
⚠️ Check $DOCS_DIR/tasks.md and review only the parts that are not N/A (actually implemented). (+ see Phase 4 Agent Status)

Review Items:
- OWASP Top 10 vulnerability assessment
- Authentication/authorization implementation verification
- Input validation and output encoding
- Sensitive data exposure check
- Dependency vulnerability check
- Security headers and CORS configuration (only if applicable parts exist)

Compile the results."
```

UX Designer (senior-ux-designer) — **Execute only when FE is included + UX Designer is required**:
```
Task prompt:
"Compare and review the FE code implemented in Phase 4 against the design mockups.

Dev server URL: $DEV_SERVER_URL

─── Comparison Process (Dual Comparison) ───

**Primary Comparison (main reference): reference.png vs FE implementation**
reference.png is the Figma original screenshot or the mockup screenshot approved in the Text Path.
This is the final source of truth for the design.

1. Check $DOCS_DIR/design/{path}/reference.png (must exist)
2. Take a screenshot of the FE implementation at $DEV_SERVER_URL using Playwright MCP (fullPage: true)
3. Measure pixel-diff metrics using mcp-design-comparison's compare_design:
   - design_path: reference.png
   - implementation_path: FE screenshot
   - output_diff_path: $DOCS_DIR/design/{path}/review-diff-p5.png
4. diff_percentage determination:
   - ≤ 5% → PASS
   - 5-15% → MINOR_REVISION (identify differing areas from the diff image)
   - > 15% → MAJOR_REVISION (possible structural issues)

**Secondary Comparison (supplementary): HTML mockup vs FE implementation**
5. Take a screenshot of $DOCS_DIR/design/{path}/index.html using Playwright MCP
6. Visually compare with the FE implementation screenshot (layout structure, component placement)
7. If HTML and FE differ, and one is closer to reference.png:
   - The side closer to reference.png is correct
   - If FE differs from reference, instruct revision

**Responsive Verification** (per state.responsive):
8. Additional capture and comparison per viewport:
   - PC only: 1920x1080
   - PC + Mobile: above + 390x844
   - PC + Mobile + Tablet: above + 768x1024
   Perform reference.png comparison for each viewport (where possible)

**When reference.png is absent** (legacy workflow compatibility):
- Perform only HTML mockup vs FE implementation comparison (secondary comparison becomes the main reference)
- If compare_design is unavailable, fall back to visual judgment
───────────────────────────────

Add a UX Design Review section to review.md in the format below:

## UX Design Review
### reference.png Comparison (Primary)
- /{path}: [PASS] / [REVISION] — diff: {N}% — detailed description
### HTML Mockup Comparison (Supplementary)
- /{path}: [PASS] / [REVISION] detailed description
### Viewport: PC (1920x1080)
- /{path}: [PASS] / [REVISION] detailed description
### Viewport: Mobile (390x844) (if applicable)
- /{path}: [PASS] / [REVISION] detailed description
### Viewport: Tablet (768x1024) (if applicable)
- /{path}: [PASS] / [REVISION] detailed description"
```

**Step 5-2 Pathological Pattern Detection** (orchestrator direct execution, only on re-review round 2+)

Compare previous and current review.md to detect 3 patterns:
- **Stagnation**: Same issue 2 consecutive times → warn "may be unsolvable within the current design"
- **Regression**: New issue not present before → warn "fix has caused a regression"
- **Oscillation** (round 3+): Round N = Round N-2 issues → warn "oscillation pattern"

When a pathological pattern is found: add a "Phase 2 design re-review" option to the gate, increment `review_iterations`.
On 3+ repetitions, strongly recommend design re-review.

---

> **Reminder**: All output in $LOCALE language.

Tech Lead (tech-lead):
```
Task prompt:
"Synthesize all review results from Phase 5, diagnose root causes, and write a fix directive for implementation agents.

Read all of the following review results:
- $DOCS_DIR/review.md (Tech Lead code review + QA results + Security review)
- $DOCS_DIR/api-contract-check.md (if present — API Contract mismatch items)

Analysis Procedure:
1. List all issues.
2. Analyze correlations between issues and group those with common causes.
   Example: 'API 500 error' (QA) + 'auth token validation missing' (Security) → middleware registration order error
3. Identify the root cause of each group at the code level (file path, line, logic).
4. Write a fix directive — assigned agent (BE/FE/DevOps), target file, fix direction, caveats.
5. Propose additional tests or structural improvements to prevent recurrence of similar issues.

Save the results to $DOCS_DIR/diagnosis.md in the following format:

## Issue Summary (N items)

### Issue Group 1: [Common Cause Summary]
Related Issues:
- [QA] Issue description
- [Security] Issue description
- [CodeReview] Issue description

Root Cause: (code-level cause analysis)
Impact Scope: (list of features/files affected by this cause)

Fix Directive:
| Assignee | File | Fix Description | Priority |
|----------|------|-----------------|----------|
| BE | src/path/file.ts:42 | Fix direction | P0 |

Regression Prevention: (tests to add or structural improvements)

(repeat for each issue group)

## Fix Priority Summary
| Rank | Issue Group | Assignee | Estimated Difficulty |
|------|-------------|----------|---------------------|
"
```

BE/FE Fix Agents (execute only when diagnosis.md contains fix directives for the respective role):
- BE: senior-backend-engineer, working directory $BACKEND_PROJECT_DIR
- FE: senior-frontend-engineer, working directory $FRONTEND_PROJECT_DIR
```
Task prompt:
"Read $DOCS_DIR/diagnosis.md and carry out the fix directives assigned to you.

Reference Documents (for determining fix direction):
- $DOCS_DIR/architecture.md — Verify original design intent (data models, API patterns, ADR)
- $DOCS_DIR/tasks.md — Verify original requirements for the relevant task

⚠️ If a fix directive in diagnosis.md conflicts with design principles in architecture.md,
   follow the diagnosis.md directive but record the conflict in the commit message.

📂 **Working Directory**: $PROJECT_DIR (BE/FE directory depending on role)

For each fix directive:
1. Check the specified file and line.
2. Apply the fix referencing the root cause analysis.
3. Add the tests specified in the Regression Prevention section.
4. Verify behavior before and after the fix.

Git Auto-Commit: commit per issue group. Format: jarfis(fix/{ROLE}): issue summary
On index.lock error, wait 3 seconds and retry (max 3 attempts).

⚠️ Only modify within the scope specified in diagnosis.md. Do not refactor outside that scope."
```

---

## Learning Candidate Detection (Orchestrator — after diagnosis.md is complete)

> Analyze the issue groups in diagnosis.md, and if fixes of the same category repeat 2 or more times,
> record them in the `learning_candidates` field. This is referenced in the Phase 6 retrospective.

```
Judgment Criteria:
- When 2 or more issue groups in diagnosis.md have similar 'Fix Description' patterns
  Example: 2 instances of "input validation missing" type → learning candidate
  Example: 3 instances of "insufficient error handling" type → learning candidate
- Prioritize inclusion if the same category appeared in diagnosis.md from previous workflows

Recording Method:
jarfis state set-nested <state_file> learning_candidates '[
  {"category": "input-validation", "count": 3, "examples": ["BE-2 parameter validation", "FE-1 form validation"]},
  {"category": "error-handling", "count": 2, "examples": ["BE-4 error response", "FE-3 error boundary"]}
]'
```
