# Phase 6: Retrospective — Agent Prompts

> This file contains agent prompts externalized from work.md.
> Auto-generated by distill.
> **Locale**: Present ALL user-facing output in $LOCALE language. Internal reasoning: English.

**Step 6-1: Retrospective** (tech-lead)

> **Context injection for TL (6-1)**: All $DOCS_DIR artifacts + .jarfis-state.json. NO project-rule (intentional exception — retrospective is process analysis, not code production). No project-context, no project-profile, no wiki-cache.

```
Task prompt:
"Conduct a retrospective of the entire JARFIS workflow.
Refer to all deliverables in $DOCS_DIR/ and .jarfis-state.json.

Write in the following format:

## Workflow Retrospective

### What Went Well (Keep)
- Agents/Phases that were efficient
- Good decisions on role skip/execution

### What to Improve (Improve)
- Bottleneck areas
- Unnecessary steps
- Missing considerations

### Action Items for Next Time (Action Items)
- Suggestions for improving the JARFIS workflow itself (only if applicable)

### Project-Specific Learnings
- Patterns, conventions, and caveats discovered in this codebase
- Reusable component/module information
- Frequently referenced file paths

### Learning Classification Criteria
Tag each learning item with a scope:
- [universal]: Principles/techniques/tool usage valid across other projects
- [project]: Patterns specific to this codebase/team/configuration only

Classification guidelines:
- Mentions specific file paths, directory structures → [project]
- Depends on specific framework version/configuration → [project]
- Qualifying expressions like 'in this project' → [project]
- General tool usage, engineering principles → [universal]

### Suggested Learnings (auto-generated section)
If the learning_candidates field exists in .jarfis-state.json, add it in the following format:

**Learning candidates derived from recurring patterns:**
| Category | Occurrences | Example | Suggested Learning |
|----------|------------|---------|-------------------|
(Organize each learning_candidates item into a table, and propose a specific learning rule for each)

Candidates are presented in candidate form so the user can explicitly promote them with /jarfis:sys-upgrade.
If learning_candidates does not exist, omit this section.

Save the result to $DOCS_DIR/retrospective.md."
```

**Step 6-2.5: Workflow Metrics Recording** (executed directly by orchestrator)

> This step is executed directly by the orchestrator without invoking an agent.
> It extracts key metrics from .jarfis-state.json and records them to TSV. **Best-effort** — only warns on failure.

Recording location: `$JARFIS_ORG_DIR/workflow-metrics.tsv`

**TSV Header** (create with this header row if the file does not exist):
```
workflow_id	project	started_at	completed_at	prd_score	review_iterations	learning_candidates_count	skipped_phases	follow_up_mode	follow_up_iteration
```

**Field Mapping** (.jarfis-state.json → TSV):

| TSV Column | Extraction Path | Default (when absent) |
|----------|-----------|----------------|
| workflow_id | `work_name` | (required — skip recording if absent) |
| project | `project_name` | "" |
| started_at | `started_at` | "" |
| completed_at | Current time (ISO8601) | — |
| prd_score | `phases.1.ratchet.prd_score` | "" |
| review_iterations | Extracted from re-review related info in `phases.5` | "0" |
| learning_candidates_count | Length of `learning_candidates` array | "0" |
| skipped_phases | Comma-separated Phase numbers where status is "skipped" | "" |
| follow_up_mode | (always empty in the main work.md workflow) | "" |
| follow_up_iteration | (always empty in the main work.md workflow) | "" |

**Execution Procedure**:
1. Check if `$JARFIS_ORG_DIR/workflow-metrics.tsv` exists → if not, create it with the header row
2. Read .jarfis-state.json → extract values according to the mapping table above
3. Compose a TSV row → append to the file
4. On success: display "Workflow metrics recorded: {workflow_id}"
5. On failure: display "Metrics recording failed (best-effort, workflow continues)" warning

> **Reminder**: All output in $LOCALE language.

**Step 6-3: Wiki 2-Track Update** (only when Org is registered, orchestrator)

> Core principle: learning = JARFIS agent/workflow improvements, wiki = Org accumulated knowledge — purposes differ, so they are independent

Execution order: **Learning extraction (Steps 6-1~6-2 first) → wiki update (this step after)**

### Track A: Text Wiki (PO, TA, QA)

1. **Deliverable Scan**: Extract accumulated knowledge from deliverables in $DOCS_DIR/
2. **Apply Extraction Criteria Checklist**:
   - Accumulate: Business rules, technology choices (ADR), API contracts, data models, design tokens, policy changes, architecture decisions
   - Exclude: Schedule-related, implementation details, review comments, progress status, inter-agent discussions
3. **Apply the "6-Month Test"**: "Will this information still be useful in 6 months?" → If No, exclude
4. **Match Existing Files**: Match against existing wiki files based on INDEX.md Summary → merge or create new
5. **Update**: Update the relevant wiki files + _index.md + INDEX.md

### Track B: DESIGN HTML Sync (only when FE is included)

1. Sync `$DOCS_DIR/design/` → `wiki/DESIGN/pages/{project}/`
2. Rules: Overwrite existing files, add new ones, preserve files that exist only in wiki
3. Auto-regenerate `wiki/DESIGN/pages/{project}/_index.html`
4. Update `wiki/DESIGN/_index.md` + `wiki/INDEX.md`

### Wiki File Frontmatter Guide

Include the following frontmatter in new/modified wiki files:
```
---
owner: {PO|TA|QA|DESIGN}
projects: [{project_name}]
created: {date}
created_by: {work_name}
last_updated: {date}
last_updated_by: {work_name}
status: active
---
```

### Semantic Search Index Update

After adding/modifying wiki files, update the wiki + works semantic search index:
```bash
# Wiki index update
python3 ~/.claude/scripts/jarfis_cli.py search index wiki
# Works index update (includes this workflow's deliverables)
python3 ~/.claude/scripts/jarfis_cli.py search index works
```
Do not halt the workflow on failure (best-effort). If the error contains `memory_insufficient` → display "Insufficient memory, skipping indexing. Update later with /jarfis:search-index --current." For other errors, display manual execution instructions.

### Update Summary Display
```
━━ Wiki Update Summary ━━
Track A (Text):
  - PO/: +1 new (domain-map.md), 1 updated (policies/refund.md)
  - TA/: +1 new (decisions/adr-003.md)
  - QA/: no changes
Track B (DESIGN):
  - pages/{project}/: 4 files synced
━━━━━━━━━━━━━━━━━━
```
