# Phase 4: Implementation — Agent Prompts

> This file contains agent prompts externalized from work.md.
> Auto-generated by distill.
> **Locale**: Present ALL user-facing output in $LOCALE language. Internal reasoning: English.

## Phase 2 Handoff (Orchestrator Pre-Work)

> Before starting Phase 4, the orchestrator reads `phases.2.handoff` from `.jarfis-state.json`
> and injects the information below into each implementation agent prompt.

```
Handoff Injection Format:
─── Phase 2 Architect Handoff ───
Key Decisions: $HANDOFF.key_decisions (each item)
Warnings: $HANDOFF.warnings (each item — must be resolved in this Phase)
Unresolved: $HANDOFF.unresolved (each item — exercise caution during implementation)
───────────────────────────────
```

> If no Handoff exists, omit this section.

## Handoff Authoring (Upon Phase 2 Completion)

> When the Phase 2 Architect completes the design, the orchestrator records the following in `.jarfis-state.json`:
> `jarfis state set-nested <state_file> phases.2.handoff '{"key_decisions":[...],"warnings":[...],"unresolved":[...]}'`
> A handoff summary is also appended to the Phase 2 completion block in work.md.

---

## Artifact Loading Checklist (Orchestrator → Agent Common Directive)

> Each implementation agent loads the files below before starting work.
> The orchestrator delivers this checklist together with the Common Implementation Rules.

```
Required (must exist):
- ✅ $DOCS_DIR/tasks.md — Task list (orchestrator error if missing)
- ✅ $DOCS_DIR/architecture.md — System design (orchestrator error if missing)

Conditional (use fallback if absent):
- ❓ $DOCS_DIR/api-spec.md — Generated when both BE and FE are needed
  → If absent: use the "API Design" section in architecture.md as the API contract reference
- ❓ $DOCS_DIR/design/ — Generated when FE + UX Designer are needed
  → If absent: implement using UX references in tasks.md + existing project component styles (text-only mode)
- ❓ token-map.json — Present only in Figma mode (phases.3.mode === 'figma')
  → If absent or unparseable: use raw hex values from the HTML mockup; record discrepancies in the commit message

※ This list follows the standard workflow. Additional artifacts may exist per project.
  Refer first to the actual files in .jarfis-state.json and $DOCS_DIR.
```

---

## Common Implementation Rules

> The rules below apply to all BE/FE/DevOps implementation agent prompts.
> Deliver this section alongside each agent prompt.
>
> **Context injection for BE/FE/DevOps**: **project-rule** + project-context + project-profile + domain compose (Skills) + tasks.md (PRIMARY) + architecture.md + api-spec.md (cond) + prd.md (REFERENCE ONLY) + design/ (FE) + scope guard + Phase 2 handoff injection.
>
> **Phase 2 Handoff**: Inject `phases.2.handoff` (key_decisions, warnings, unresolved) from `.jarfis-state.json` into each implementation agent prompt. See "Phase 2 Handoff" section above.

> **Reminder**: All output in $LOCALE language.

```
Common Implementation Rules:
- Work based on the files specified in each task's 'Target Files' (no unnecessary codebase exploration)
- Follow the project profile conventions if a project profile exists
- Satisfy every task's 'Completion Criteria', 'Tests', and 'Security' requirements
- prd.md is REFERENCE ONLY — follow tasks.md as the primary implementation guide

─── Scope Guard (BE/FE/DevOps only) ───
요구사항에 명시된 범위만 구현한다. 범위 밖의 추상화, 일반화, 선제적 리팩토링은 하지 않는다.
───────────────────────────────

Git Auto-Commit (per task):
- On task completion, git add changed files + commit
- Commit format: jarfis({ROLE}-N): one-line summary of task completion criteria
- On index.lock error, wait 3 seconds and retry (max 3 attempts)
- On completion, report in the format [TASK_DONE: {ROLE}-N]
```

---

**Step 4-0: Security Pre-Review** (senior-security-engineer)

> **Context injection for Security**: .wiki-cache.md + architecture.md + tasks.md. No project-rule, no project-context, no project-profile.

```
Task prompt:
"Read $DOCS_DIR/architecture.md and $DOCS_DIR/tasks.md, then write pre-implementation security guidelines.

Include:
- Authentication/authorization implementation guide
- Input validation rules
- Sensitive data handling policy
- API security checklist
- List of security vulnerabilities to watch for (based on OWASP Top 10)

Organize these guidelines so that BE/FE/DevOps agents can reference them during implementation."
```

**Step 4-0.5: Test-First Writing (TDD Red Phase)** (senior-qa-engineer) — **Conditional Execution**

> **Context injection for QA (4-0.5)**: **project-rule** + project-profile (BE/FE) + test-strategy.md + tasks.md + api-spec.md (cond) + architecture.md. No project-context, no wiki-cache.

> Execution conditions: Run only when ALL of the following are met.
> 1. `$DOCS_DIR/test-strategy.md` exists
> 2. test-strategy.md contains 3 or more P0 test scenarios
> 3. A unit test framework exists in the project (verify via project profile or package.json/requirements.txt, etc.)
>
> If conditions are not met, skip this Step and proceed to Step 4-1.
> Record `phases.4.tdd_enabled: true|false` in `.jarfis-state.json`.

> **Reminder**: All output in $LOCALE language.

```
Task prompt:
"Convert the P0/P1 test scenarios from $DOCS_DIR/test-strategy.md into executable test code.

📂 BE test directory: $BACKEND_PROJECT_DIR (follow project test conventions)
📂 FE test directory: $FRONTEND_PROJECT_DIR (follow project test conventions)
⚠️ Do not write tests for parts marked N/A in tasks.md.

References:
- $DOCS_DIR/test-strategy.md — Original test scenarios (convert these scenarios into code)
- $DOCS_DIR/api-spec.md — API contract (expected request/response values for BE tests, if present)
- $DOCS_DIR/architecture.md — Data models, business rules (basis for edge case inference)
- $DOCS_DIR/tasks.md — 'Target Files' for each task (basis for import path configuration)
- $BE_PROJECT_PROFILE / $FE_PROJECT_PROFILE — Test framework, directory structure, conventions

Test Writing Rules:
1. Since no implementation exists yet, all tests are expected to FAIL or produce import errors (RED state).
   - However, the test files themselves must be free of syntax errors.
2. Set import paths based on the 'Target Files' in tasks.md.
   - Example: tasks.md has 'src/services/payment.ts' → import { PaymentService } from '...'
3. Document the business intent of each test function via docstring/comment.
4. Actively include edge cases:
   - Empty input, null/undefined, boundary values, unauthorized access, concurrency, timeouts
   - Especially edge cases inferable from architecture.md business rules, even if not explicitly stated in test-strategy.md
5. Test behavior, not implementation details.
   - Base tests on public interfaces, not internal implementation details (private methods, internal state)
6. Follow existing test patterns in the project.
   - If the project profile specifies test conventions, follow them.
   - If existing test files exist, reference their style (fixtures, naming conventions, etc.)

Deliverables:
- BE tests: Create files in the project's test directory
- FE tests: Create files in the project's test directory
- Comment at the top of each file: // [JARFIS TDD] Auto-generated in Phase 4-0.5 — implementation agents must not modify these tests

Git Auto-Commit: jarfis(QA-test): pre-write test code based on test-strategy.md"
```

---

Backend (senior-backend-engineer) — **SKIP if Backend Tasks are N/A**:
```
Task prompt:
"Implement the Backend Tasks from $DOCS_DIR/tasks.md in dependency order.

📂 Working directory: $BACKEND_PROJECT_DIR

References: tasks.md (tasks), api-spec.md (API Contract, if present), architecture.md (data models/patterns)

$PROJECT_CONTEXT
$BE_PROJECT_PROFILE

─── TDD Green Phase + Ratchet (when Step 4-0.5 was executed) ───
Test code written by QA in Phase 4-0.5 exists.
1. Before implementing each task, run the relevant tests to confirm RED (failing) state.
2. Implement the task.
3. After implementation, **run the full test suite** and report results:
   [TEST_RESULT: passed={N}, failed={N}, total={N}]
4. If tests do not pass, fix the implementation.

📊 **Ratchet Rule**: The orchestrator tracks the test pass rate (passed/total).
   If the pass rate drops below the level at the previous task's completion, reimplementation will be ordered.

⚠️ Do not modify test code — if you believe a test is incorrect,
   report it in the format [TEST_ISSUE: {test_name} — {reason}].
   You must include specific evidence for why the test is wrong.
   The orchestrator will request a re-review from QA.

⚠️ If you must modify a test file:
   The commit message must include [TEST_MODIFIED: {filename} — {reason}].
   Phase 5 QA will verify the validity of the modification.

※ If Step 4-0.5 was skipped (tdd_enabled=false), ignore this block.
───────────────────────────────

Follow the Common Implementation Rules above. Commit format: jarfis(BE-N):"
```

Frontend (senior-frontend-engineer) — **SKIP if Frontend Tasks are N/A**:
```
Task prompt:
"Implement the Frontend Tasks from $DOCS_DIR/tasks.md in dependency order.

📂 Working directory: $FRONTEND_PROJECT_DIR

References: tasks.md (tasks + UX references), api-spec.md (type definitions/API call reference, if present)

─── Design Contract (Design Mockup Reference Rules) ───
Reference the artifacts in $DOCS_DIR/design/ as a Visual Contract.

Required reference files (if present):
- $DOCS_DIR/design/{path}/index.html — HTML mockup (pixel-perfect layout/interaction reference)
- $DOCS_DIR/design/{path}/reference.png — Comparison baseline screenshot (Figma original or Text Path capture)
  ⚠️ reference.png is the final visual authority. If HTML and reference.png differ, follow reference.png.

Conditional reference files (Figma-driven: state phases.3.mode === 'figma'):
- $DOCS_DIR/design/token-map.json — Figma hex → project CSS/SCSS variable mapping
  Convert CSS variables in the HTML mockup to actual project variables. Use raw values for unmapped tokens.
- $DOCS_DIR/design/assets/ — Image assets (photos/, icons/, illustrations/, backgrounds/)
  Copy to the appropriate project location (public/, assets/, etc.) for use.
- $DOCS_DIR/design/{path}/figma-spec.yaml — Figma YAML source (for detailed spec verification)
- $DOCS_DIR/design/{path}/section-map.json — Section structure (for understanding layout intent)

Common rules:
- URL → mockup mapping: /{path} → $DOCS_DIR/design/{path}/index.html
- Do not copy-paste HTML/CSS directly — implement using the project's tech stack (Vue, React, SCSS, etc.)
- Responsive: if the HTML mockup is responsive, apply the same breakpoints
- <!-- COMMON: {name} --> comments: use existing shared components from the project

─── Design Application Verification Checklist (MANDATORY) ───
Before reporting task completion, verify ALL of the following:
□ Every Vue component has a <style> block (scoped or module)
□ Layout matches HTML mockup: flex direction, gap, padding, margins
□ Colors use token-map.json variables where mapped, raw hex for unmapped
□ Typography matches: font-family, font-size, font-weight, line-height
□ Status badges/pills have correct background + text colors
□ Filter buttons show active/inactive states with correct styling
□ Empty states, loading states, error states all have appropriate styling
□ If any checkbox is NOT met, fix it before committing.
This checklist has EQUAL priority with functional implementation.
Do NOT commit code that works functionally but lacks styling.
──────────────────────────────────────────
───────────────────────────────

$PROJECT_CONTEXT
$FE_PROJECT_PROFILE

─── TDD Green Phase + Ratchet (when Step 4-0.5 was executed) ───
Test code written by QA in Phase 4-0.5 exists.
1. Before implementing each task, run the relevant tests to confirm RED (failing) state.
2. Implement the task.
3. After implementation, **run the full test suite** and report results:
   [TEST_RESULT: passed={N}, failed={N}, total={N}]
4. If tests do not pass, fix the implementation.

📊 **Ratchet Rule**: The orchestrator tracks the test pass rate (passed/total).
   If the pass rate drops below the level at the previous task's completion, reimplementation will be ordered.

⚠️ Do not modify test code — if you believe a test is incorrect,
   report it in the format [TEST_ISSUE: {test_name} — {reason}].
   You must include specific evidence for why the test is wrong.
   The orchestrator will request a re-review from QA.

⚠️ If you must modify a test file:
   The commit message must include [TEST_MODIFIED: {filename} — {reason}].
   Phase 5 QA will verify the validity of the modification.

※ If Step 4-0.5 was skipped (tdd_enabled=false), ignore this block.
───────────────────────────────

Follow the Common Implementation Rules above. Commit format: jarfis(FE-N):"
```

DevOps (senior-devops-sre-engineer) — **SKIP if DevOps Tasks are N/A**:
```
Task prompt:
"Implement the DevOps Tasks from $DOCS_DIR/tasks.md in dependency order.

References: tasks.md (category/target files per task), architecture.md (infrastructure configuration)

⚠️ Check the 'Category' of each task:
- Category A (local creation): Dockerfile, CI/CD, IaC, etc. → create directly
- Category B (cloud operations): → document step-by-step execution guide in $DOCS_DIR/infra-runbook.md

Follow the Common Implementation Rules above. Commit format: jarfis(DevOps-N):"
```
