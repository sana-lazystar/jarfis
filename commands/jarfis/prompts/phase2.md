# Phase 2: Architecture & Planning — Agent Prompts

> This file contains agent prompts externalized from work.md.
> Auto-generated by distill.
> **Locale**: Present ALL user-facing output in $LOCALE language. Internal reasoning: English.

**Step 2-(-1): TA/QA Wiki Reference** (Only when Org is registered)

> **Context injection**: Specific wiki sub-files ONLY. No project-rule, no project-context, no project-profile, no wiki-cache.

TA (technical-architect):
```
Wiki Reference Guidelines:
"Check the following in the TA/ wiki:
- decisions/ (existing ADRs — technology choices, architecture patterns)
- api-contracts/ (existing API contracts)
- data-models/ (existing data model definitions)

Maintain consistency with existing ADRs. Write new ADRs inline within architecture.md."
```

QA (senior-qa-engineer):
```
Wiki Reference Guidelines:
"Check the following in the QA/ wiki:
- test-standards.md (test criteria, coverage targets)
- regression-checklist.md (regression test checklist)

Establish a test strategy that maintains consistency with existing test standards."
```

---

**Step 2-0: Impact Analysis** (technical-architect)

> **Context injection for TA**: project-context + project-profile + .wiki-cache.md + prd.md. No project-rule.

```
Task prompt:
"Read $DOCS_DIR/prd.md and analyze the scope of impact on the existing codebase.

$PROJECT_CONTEXT
$BE_PROJECT_PROFILE
$FE_PROJECT_PROFILE

Analysis items:
1. **List of affected files/modules** — explore the codebase and list with actual file paths
2. **List of dependent external services/APIs**
3. **Whether DB schema changes are needed** — specify if migrations are required
4. **Impact on other features when shared components change** — whether shared modules being modified are used elsewhere
5. **Whether there are Breaking Changes** — whether existing API consumers or existing data formats are affected

Save the results to $DOCS_DIR/impact-analysis.md."
```

**Step 2-1: System Architecture Design** (technical-architect)
```
Task prompt:
"Read $DOCS_DIR/prd.md and $DOCS_DIR/impact-analysis.md and design the system architecture.

Include the following:
1. System architecture diagram (Mermaid)
2. Technology stack selection with rationale
3. Data model design
4. API design (key endpoints)
5. Infrastructure configuration
6. Security architecture
7. Scalability strategy

8. **Architecture Decision Records (ADR)** — document each major design decision in the following format:

## Architecture Decision Records

### ADR-001: [Decision Title]
- **Status**: Approved
- **Context**: What problem needed to be solved
- **Alternatives considered**:
  | Alternative | Pros | Cons |
  |-------------|------|------|
  | Option A | ... | ... |
  | Option B | ... | ... |
- **Decision**: The chosen option
- **Rationale**: Trade-off analysis results
- **Impact**: What changes or constraints result from this decision

Write ADRs for at minimum: technology stack selection, data model structure, and key architecture patterns.

Save the results to $DOCS_DIR/architecture.md."
```

**Step 2-1.5: API Specification** (technical-architect + tech-lead sequential)

> **Context injection for TA (API Spec)**: project-context + project-profile + .wiki-cache.md + architecture.md. No project-rule.
> **Context injection for TL (API Review)**: **project-rule** + project-profile + .wiki-cache.md + api-spec.md + architecture.md. No project-context.

Architect (technical-architect):
```
Task prompt:
"Based on $DOCS_DIR/architecture.md, write a detailed API specification (BE-FE API Contract).

Include the following for each endpoint:
- ## [Method] [Path] (e.g., ## POST /api/v1/boards)
- Description, Authentication (permissions/roles)
- Request parameters table (Parameter, Location, Type, Required, Description)
- Request Body Example (JSON)
- Response Example (JSON)
- Error Responses table (Code, Error Key, Message, Description)

Write clearly enough that FE can directly convert it into type definitions.
Save the results to $DOCS_DIR/api-spec.md."
```

Tech Lead (tech-lead) — api-spec.md review:
```
Task prompt:
"Review $DOCS_DIR/api-spec.md.

Verification criteria:
- Whether it is consistent with the existing codebase's API patterns/naming conventions
- Whether parameter types and response schemas are clear enough for FE to directly convert into type definitions
- Whether there are any missing error cases
- Whether authentication/authorization requirements are clear

If corrections are needed, apply them directly to $DOCS_DIR/api-spec.md."
```

**Step 2-2: Task Decomposition** (tech-lead)

> **Context injection for TL**: **project-rule** + project-context + project-profile + .wiki-cache.md + architecture.md + api-spec.md + prd.md.

```
Task prompt:
"Read $DOCS_DIR/prd.md, architecture.md, impact-analysis.md, test-strategy.md, api-spec.md (if exists) and decompose the tasks.

⚠️ Mark sections for roles listed as 'Not Required' in the PRD 'Required Roles' table as 'N/A'.
When project profiles ($BE/$FE_PROJECT_PROFILE) exist, specify target files concretely.

Organize tasks per role (BE/FE/DevOps) with the following fields:
- Description, related APIs (refer to api-spec.md), target files (new/modified), dependencies
- Completion criteria, tests (inline from test-strategy.md), security (refer to architecture.md security section)
- Estimated size (S/M/L), UX reference (FE only), category A/B (DevOps only)

Additional sections: Shared/Cross-cutting + dependency summary (Mermaid)

Responsive effort considerations (refer to the responsive field in .jarfis-state.json):
- PC only → base effort
- PC+Mobile → FE effort ~1.3x
- PC+Mobile+Tablet → FE effort ~1.5x

Save the results to $DOCS_DIR/tasks.md."
```

**Step 2-3: Test Strategy** (senior-qa-engineer)

> **Context injection for QA**: **project-rule** + project-profile + .wiki-cache.md + prd.md + architecture.md + tasks.md. No project-context.
> **Reminder**: All output in $LOCALE language.

```
Task prompt:
"Read $DOCS_DIR/prd.md, $DOCS_DIR/tasks.md, $DOCS_DIR/api-spec.md (if exists) and establish a test strategy.
⚠️ Exclude tests for parts marked as N/A in tasks.md.

Include the following:

## Test Pyramid

### Unit Tests
| Target module/function | Test items | Priority |
|----------------------|------------|----------|
| (e.g., createPost) | Successful creation, empty title, no permission | P0 |

### Integration Tests
| Integration target | Test items | Priority |
|-------------------|------------|----------|
| (e.g., API → DB) | CRUD normal operation, transaction rollback | P0 |

### E2E Tests (Critical Path)
| Scenario | Steps | Priority |
|----------|-------|----------|
| (e.g., Post creation flow) | Login → Create → Save → Verify | P0 |

## Edge Case List
- (List edge cases per feature)

## Performance Test Criteria
- Specify Pass/Fail criteria referencing the PRD's Performance Budget

Save the results to $DOCS_DIR/test-strategy.md."
```

**Step 3-0: HTML Mockup Creation/Revision** (senior-ux-designer) — Only when FE + UX Designer are required
```
Task prompt:
"Read $DOCS_DIR/ux-direction.md and create HTML mockups.

URL → file mapping:
- /{path} → $DOCS_DIR/design/{path}/index.html or {path}.html

Requirements for each HTML file:
- Insert templates/design-html-meta.md meta comment at the top
- Must be standalone runnable (only external CDNs allowed, no local dependencies)
- Visually represent interaction patterns from ux-direction.md
- Responsive scope: refer to the responsive field in .jarfis-state.json

Final deliverables:
- $DOCS_DIR/design/_index.html (table of contents page for all mockups)
- HTML mockup files for each URL"
```

**Step 3-1: PO ↔ Designer Feedback Loop** (max 3 rounds)

PO (senior-product-owner):
```
Task prompt:
"Review the HTML mockups in the $DOCS_DIR/design/ directory.
Verification items against $DOCS_DIR/prd.md and $DOCS_DIR/ux-direction.md:
- Whether all user stories from the PRD are reflected
- Whether interaction patterns from ux-direction.md are implemented
- Whether the UX is suitable for achieving business goals
- Whether there are any missing screens or edge cases

If there is feedback, describe it specifically (per page)."
```

Designer (senior-ux-designer) — Applying PO feedback:
```
Task prompt:
"Revise the HTML mockups to reflect the PO's feedback.
Feedback: [PO feedback content]
After revision, summarize the changes."
```
